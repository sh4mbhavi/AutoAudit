name: "Short Test Workflow"

on:
  push:
    paths:
      # Phase 10: this named "short-test.yml", but the file is
      # ops.short-test.yml, so the workflow could never trigger -- and it is
      # the canary for ops.workflow-cleanup, meaning the cleanup behaviour
      # had never been exercised by its own test.
      - ".github/workflows/ops.short-test.yml"

jobs:
  quick-job:
    runs-on: ubuntu-latest
    steps:
      - name: Quick echo
        run: echo "This is a short workflow for testing cleanup"

  #
