"""Policy lifecycle tooling.

Nothing in here is imported by the engine or the API at runtime. These are
operator tools that read and, only under explicit gates, rewrite the declarative
policy artifacts under ``engine/policies/``.
"""
