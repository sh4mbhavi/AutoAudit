# Ignore virtual environment in AutoAudit
.venv/
venv/
env/
**/.venv/
**/venv/
**/env/
.DS_Store

# Python caches
**/__pycache__/
**/*.py[codz]
**/.pytest_cache/
**/.ruff_cache/
**/.mypy_cache/

# Node / frontend artifacts
**/node_modules/
**/build/
**/dist/
**/coverage/

# Editor / OS
.idea/
.vscode/

# Environment files (keep examples)
.env
.env.*
!.env.example
**/.env
**/.env.*
!**/.env.example

# Engine legacy generated outputs
engine/legacy/engine/autoaudit_reports.json

# Local SharePoint runtime override
docker-compose.sharepoint.override.yml

# Local Purview/compliance runtime override. Phase 8 added the overlay and its
# .example but not this rule, so a file the example tells operators to fill with
# real certificate host paths was stageable.
docker-compose.compliance.override.yml
